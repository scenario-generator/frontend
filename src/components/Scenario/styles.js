import Colors from '../../constants/styles/colors'

let styles = {
  scenario: {
    display: 'flex',
    justifyContent: 'center',
    flexFlow: 'row wrap',
    marginTop: 15,
  },
}

export default styles